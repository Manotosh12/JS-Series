const mystates = [
    "mp",
    "up",
    "mm",
    1947,
    "tamil",
    "nadu",
];

// mostly in js all are uses for_each loop to replacing for loop
// for_each loop actually not a loop actually it's a method(works like loop)
// to avoid the write whole lots of stuff in or simplify for loop we use for_each
// actually a callback function

mystates.forEach((s) => (console.log(s)));
