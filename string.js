// `` : this is called string literals we can define string as like we wish
// with $ we use interpolation which very mush use in React
var name = `Mono`;
var fullName = `${name} saha`;

console.log(fullName);
console.log(fullName.charAt(0));
console.log(fullName.endsWith("a"));
console.log(fullName.includes("Mono"));
console.log(fullName.toLocaleUpperCase());